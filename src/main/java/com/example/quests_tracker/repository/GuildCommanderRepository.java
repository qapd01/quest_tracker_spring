package com.example.quests_tracker.repository;

import com.example.quests_tracker.entities.GuildCommander;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GuildCommanderRepository extends JpaRepository<GuildCommander, Long> {
}
